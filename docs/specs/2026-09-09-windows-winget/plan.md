# Native Windows and WinGet plan

- [x] Make process and executable lookup platform-correct, including Windows `PATHEXT`.
- [x] Add native Windows clipboard and browser integrations with focused tests.
- [x] Add a Windows job to CI and a Windows x86-64 ZIP to the release matrix.
- [x] Document WinGet and native Windows support.
- [x] Bump the release version and add its changelog entry.
- [ ] Run formatting, lint, tests, release build, and a Windows-target compile check.
- [ ] Publish the release and verify its Windows asset and checksum.
- [ ] Submit and validate the initial `Jhao0413.Diple` manifest in `microsoft/winget-pkgs`.
